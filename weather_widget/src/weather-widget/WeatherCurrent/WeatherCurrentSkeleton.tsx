import { Skeleton } from "@mui/material";

export const WeatherCurrentSkeleton = () => {
  return <Skeleton variant="rectangular" width={340} height={153} />;
};

export default WeatherCurrentSkeleton;
